# Autorel configuration for tempurview
# https://github.com/mhweiner/autorel

# Only release from main branch
branches:
  - name: 'main'

# Don't publish to npm (this is a Rust project)
publish: false

# Run tests before creating a release
preRun: cargo test --lib
